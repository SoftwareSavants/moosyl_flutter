import 'package:flutter/material.dart';

class SoftPayPage extends StatelessWidget {
  const SoftPayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
