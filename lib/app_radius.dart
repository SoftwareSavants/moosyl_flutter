import 'package:flutter/material.dart';

class AppRadius {
  static const none = BorderRadius.all(Radius.circular(0));
  static const xs = BorderRadius.all(Radius.circular(2));
  static const sm = BorderRadius.all(Radius.circular(4));
  static const md = BorderRadius.all(Radius.circular(6));
  static const lg = BorderRadius.all(Radius.circular(8));
  static const xl = BorderRadius.all(Radius.circular(12));
  static const xxl = BorderRadius.all(Radius.circular(16));
  static const xxxl = BorderRadius.all(Radius.circular(20));
  static const fourXl = BorderRadius.all(Radius.circular(32));
  static const full = BorderRadius.all(Radius.circular(9999));
}
